<%= simple_form_for @ad do |f| %>
	<%= f.input :title %>
	<%= f.input :category %>
	<%= f.input :description %>
	<%= f.input :localization %>
	<%= f.input :phone %>
	<%= f.input :price %>
	<%= f.input :picture, as: :file %>

	<%= f.button :submit %>
<% end %>